<%= form_for(@employee_phone) do |f| %>
  <% if @employee_phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee_phone.errors.count, "error") %> prohibited this employee_phone from being saved:</h2>

      <ul>
      <% @employee_phone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :employee_id %><br>
    <%= f.text_field :employee_id %>
  </div>
  <div class="field">
    <%= f.label :phone %><br>
    <%= f.text_field :phone %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
