<%= form_for(@vendor_phone) do |f| %>
  <% if @vendor_phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vendor_phone.errors.count, "error") %> prohibited this vendor_phone from being saved:</h2>

      <ul>
      <% @vendor_phone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :vendor_id %><br>
    <%= f.text_field :vendor_id %>
  </div>
  <div class="field">
    <%= f.label :phone %><br>
    <%= f.text_field :phone %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
