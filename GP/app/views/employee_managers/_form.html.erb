<%= form_for(@employee_manager) do |f| %>
  <% if @employee_manager.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee_manager.errors.count, "error") %> prohibited this employee_manager from being saved:</h2>

      <ul>
      <% @employee_manager.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :employee_id %><br>
    <%= f.text_field :employee_id %>
  </div>
  <div class="field">
    <%= f.label :crew_id %><br>
    <%= f.text_field :crew_id %>
  </div>
  <div class="field">
    <%= f.label :start_date %><br>
    <%= f.date_select :start_date %>
  </div>
  <div class="field">
    <%= f.label :end_date %><br>
    <%= f.date_select :end_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
