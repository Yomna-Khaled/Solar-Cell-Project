<%= form_for(@saled_panel) do |f| %>
  <% if @saled_panel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@saled_panel.errors.count, "error") %> prohibited this saled_panel from being saved:</h2>

      <ul>
      <% @saled_panel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :solar_panel_id %><br>
    <%= f.text_field :solar_panel_id %>
  </div>
  <div class="field">
    <%= f.label :buyer_id %><br>
    <%= f.text_field :buyer_id %>
  </div>
  <div class="field">
    <%= f.label :totalPrice %><br>
    <%= f.text_field :totalPrice %>
  </div>
  <div class="field">
    <%= f.label :totalPower %><br>
    <%= f.text_field :totalPower %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
